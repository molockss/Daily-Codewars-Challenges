// Re-arrange array

/* it is your job to re-arrange the array so that the animal is the right way round (head, body, tail). 

Example below;

("Testing for fixed tests", () => {
 ["tail", "body", "head"])

 should equal
 
 ["head", "body", "tail"]);

  
 Solution:  */


 function fixTheMeerkat(arr) {
    const reversed = arr.reverse();
    return reversed 
   }

// I set the variablee reversed equal to the reverse of whateever the array is

// I then returned the variable